Installing the packages for development purpose:
Run the command : "npm install" in the project.

Running the project locally:
Run the command : "ng serve --open" to build the project and open it on the port 4200.

